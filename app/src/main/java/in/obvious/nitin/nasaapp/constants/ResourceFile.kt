package `in`.obvious.nitin.nasaapp.constants

import `in`.obvious.nitin.nasaapp.R

/**
 * @author Nitin Khanna
 * @date 13/09/2022
 */
enum class ResourceFile(val file: Int, val isList: Boolean) {
    IMAGE_DATA(R.raw.data, true),
}